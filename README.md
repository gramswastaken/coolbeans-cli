# CB Cli

inspired by caelestia-cli for that project, have all of the control be in a python program instead of a bunch of bash scripts
maybe its easier to maintain

swappy must have it's own config file at xdg_config_home/swappy/config with save_dir being set to whatever the screenshot dir is

